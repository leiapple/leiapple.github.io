source "https://rubygems.org"

gem "jekyll", "~> 4.3.2"

group :jekyll_plugins do
  gem "jekyll-scholar"
  gem "jekyll-paginate"
  gem "jekyll-sitemap"        # ← Add this
  gem "jekyll-feed"           # ← Add this (likely also missing)
  gem "jemoji"                # ← Add this (likely also missing)
  gem "jekyll-github-metadata"
end

# Required for Ruby 3.4.0 compatibility
gem "csv"
gem "base64"
gem "faraday-retry"
gem "webrick"